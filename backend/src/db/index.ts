/**
 * Database Exports
 *
 * Central export point for database connection and schemas
 */

export { db, closeDatabase } from './connection';

// Import all schemas when needed
// export * as schema from './schema';
